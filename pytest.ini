[pytest]
testpaths = tests
markers =
    moss_integration: mark test as requiring live Moss credentials (skipped if unconfigured)
    gemini_integration: mark test as requiring live Gemini credentials (skipped if unconfigured)
    postgres: mark test as requiring a configured PostgreSQL database
asyncio_mode = auto
